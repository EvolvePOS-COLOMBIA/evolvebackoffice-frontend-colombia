import { BusinessModulePlaceholder } from "@/features/business/components/business-module-placeholder"

export function PeoplePage() {
  return (
    <BusinessModulePlaceholder
      title="People"
      description="Keep staff, permissions, customers, and assignment flows scoped to the current tenant."
    />
  )
}
